name: release-desktop

"on":
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  dist:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build desktop app
        run: bun run build:desktop

      - name: Package desktop app
        run: bun --filter desktop dist

      - name: Upload packaged output
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: apps/desktop/dist-electron/*

  release:
    needs: dist
    runs-on: ubuntu-latest
    steps:
      - name: Download packaged artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          # No merge-multiple: each OS artifact stays in its own
          # dist/<name>/ subdirectory so same-named files can't collide.
          merge-multiple: false

      - name: Create draft GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          # Recursive: electron-builder also emits unpacked/ metadata dirs.
          files: dist/**/*
