<script lang="ts">
	import { Tabs as TabsPrimitive } from "bits-ui";
	import { cn } from '@np/core';

	let {
		ref = $bindable(null),
		value = $bindable(""),
		class: className,
		...restProps
	}: TabsPrimitive.RootProps = $props();
</script>

<TabsPrimitive.Root
	bind:ref
	bind:value
	data-slot="tabs"
	class={cn("group/tabs flex data-[orientation=horizontal]:flex-col", className)}
	{...restProps}
/>
