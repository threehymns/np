<script lang="ts">
	import { Dialog as DialogPrimitive } from "bits-ui";
	import { cn } from '@np/core';

	let {
		ref = $bindable(null),
		class: className,
		...restProps
	}: DialogPrimitive.OverlayProps = $props();
</script>

<DialogPrimitive.Overlay
	bind:ref
	data-slot="dialog-overlay"
	class={cn("data-open:animate-in data-closed:animate-out data-closed:fade-out-0 data-open:fade-in-0 bg-background/80 duration-100 backdrop-blur-md fixed inset-0 isolate z-50", className)}
	{...restProps}
/>
